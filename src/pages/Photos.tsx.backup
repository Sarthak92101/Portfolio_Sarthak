import { useState, useEffect, useCallback } from "react";
// Temporarily comment out framer-motion imports
// import { AnimatePresence, motion, useScroll, useTransform, useSpring } from "framer-motion";
import { ArrowLeft, ChevronLeft, ChevronRight, Download, Heart, RotateCw, Search, Share2, X, Camera, Sparkles } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent } from "@/components/ui/dialog";
import { useNavigate } from "react-router-dom";

interface Photo {
  id: number;
  src: string;
  alt: string;
  title: string;
  category: string;
  date?: string;
}

const photos: Photo[] = [
  // Original photos with enhanced descriptions
  {
    id: 1,
    src: "/photos/Snapchat%20for%20Web%202026-4-4%20at%2012_59_14%20pm.png",
    alt: "Creative self-portrait with artistic lighting",
    title: "Digital Self",
    category: "Self-Portrait",
    date: "April 2026"
  },
  {
    id: 2,
    src: "/photos/Snapchat-1298872259.jpg",
    alt: "Casual lifestyle moment",
    title: "Urban Explorer",
    category: "Lifestyle",
    date: "Recent"
  },
  {
    id: 3,
    src: "/photos/Snapchat-1604176005.jpg",
    alt: "Intimate personal moment",
    title: "Reflections",
    category: "Portrait",
    date: "2025"
  },
  {
    id: 4,
    src: "/photos/Snapchat-1841863640.jpg",
    alt: "Artistic portrait composition",
    title: "Light & Shadow",
    category: "Artistic",
    date: "2025"
  },
  {
    id: 5,
    src: "/photos/Snapchat-79238901.jpg",
    alt: "Candid personal photograph",
    title: "Authentic Moments",
    category: "Candid",
    date: "2025"
  },
  {
    id: 6,
    src: "/photos/WhatsApp%20Image%202025-01-09%20at%207.12.24%20PM%20(2).jpeg",
    alt: "Winter memory captured",
    title: "Winter Glow",
    category: "Seasonal",
    date: "January 2025"
  },
  {
    id: 7,
    src: "/photos/WhatsApp%20Image%202025-01-15%20at%206.16.46%20PM.jpeg",
    alt: "Evening ambiance portrait",
    title: "Golden Hour",
    category: "Portrait",
    date: "January 2025"
  },
  {
    id: 8,
    src: "/photos/WhatsApp%20Image%202025-02-03%20at%2012.45.35%20PM.jpeg",
    alt: "Dynamic movement capture",
    title: "Motion Blur",
    category: "Action",
    date: "February 2025"
  },
  {
    id: 9,
    src: "/photos/WhatsApp%20Image%202026-04-04%20at%2012.44.53%20(1).jpeg",
    alt: "Contemporary lifestyle shot",
    title: "Modern Life",
    category: "Lifestyle",
    date: "April 2026"
  },
  {
    id: 10,
    src: "/photos/WhatsApp%20Image%202026-04-04%20at%2012.44.54%20(1).jpeg",
    alt: "Everyday life documentation",
    title: "Daily Chronicles",
    category: "Documentary",
    date: "April 2026"
  },
  {
    id: 11,
    src: "/photos/WhatsApp%20Image%202026-04-04%20at%2012.44.56.jpeg",
    alt: "Current era snapshot",
    title: "2026 Vision",
    category: "Contemporary",
    date: "April 2026"
  },
  {
    id: 12,
    src: "/photos/WhatsApp Image 2026-04-04 at 12.44.59.jpeg",
    alt: "Latest creative capture",
    title: "Future Forward",
    category: "Artistic",
    date: "April 2026"
  },

  // New photos added
  {
    id: 13,
    src: "/photos/07F9D176-2807-4364-B8C6-078C7DA7ED46_1_105_c.jpeg",
    alt: "Spontaneous moment of joy and laughter",
    title: "Joyful Expression",
    category: "Candid",
    date: "April 2026"
  },
  {
    id: 14,
    src: "/photos/11992AE3-A0BA-4386-8547-69E283614776_1_105_c.jpeg",
    alt: "Peaceful contemplation in natural surroundings",
    title: "Serene Thoughts",
    category: "Nature",
    date: "April 2026"
  },
  {
    id: 15,
    src: "/photos/159D4FA2-0135-41AE-B0AB-B767C8E2E285_1_105_c.jpeg",
    alt: "Creative self-expression through photography",
    title: "Creative Soul",
    category: "Self-Portrait",
    date: "April 2026"
  },
  {
    id: 16,
    src: "/photos/1EC26B00-4881-48AB-942E-AB8214E1B7C6.jpeg",
    alt: "Urban exploration and city life",
    title: "City Lights",
    category: "Urban",
    date: "April 2026"
  },
  {
    id: 17,
    src: "/photos/2542FAA1-127C-42DB-8B19-8F440842EEA6_1_105_c.jpeg",
    alt: "Moments of quiet reflection",
    title: "Inner Peace",
    category: "Portrait",
    date: "April 2026"
  },
  {
    id: 18,
    src: "/photos/2D670F59-E5CA-4BA2-9776-4C3CC1B73C95_1_105_c.jpeg",
    alt: "Dynamic energy and movement",
    title: "Dynamic Flow",
    category: "Action",
    date: "April 2026"
  },
  {
    id: 19,
    src: "/photos/352EC82C-D78C-4BE7-89B6-6FEF0EB02FE8_1_105_c.jpeg",
    alt: "Artistic composition with dramatic lighting",
    title: "Dramatic Light",
    category: "Artistic",
    date: "April 2026"
  },
  {
    id: 20,
    src: "/photos/3D825066-DD1D-4059-A730-56E5F8A94299_1_105_c.jpeg",
    alt: "Everyday moments that matter",
    title: "Life's Details",
    category: "Documentary",
    date: "April 2026"
  },
  {
    id: 21,
    src: "/photos/3D9998CF-E23F-452F-BB0F-99BCCA4B2A98_1_105_c.jpeg",
    alt: "Modern lifestyle and contemporary vibes",
    title: "Contemporary Vibes",
    category: "Lifestyle",
    date: "April 2026"
  },
  {
    id: 22,
    src: "/photos/44F701DB-F1F1-4CD6-B766-F2E1918402FA_1_105_c.jpeg",
    alt: "Golden hour magic captured",
    title: "Golden Moments",
    category: "Seasonal",
    date: "April 2026"
  },
  {
    id: 23,
    src: "/photos/4B4868EA-0024-46F1-89C8-D0C8041435E4_1_105_c.jpeg",
    alt: "Authentic emotions and genuine expressions",
    title: "True Emotions",
    category: "Candid",
    date: "April 2026"
  },
  {
    id: 24,
    src: "/photos/4DA53178-5F4A-4A30-848E-C54C2234C81A_1_105_c.jpeg",
    alt: "Urban adventures and city exploration",
    title: "Urban Adventures",
    category: "Urban",
    date: "April 2026"
  },
  {
    id: 25,
    src: "/photos/4E5482E0-93AA-4D9C-A611-804DDEC425A1_1_105_c.jpeg",
    alt: "Natural beauty and outdoor serenity",
    title: "Natural Harmony",
    category: "Nature",
    date: "April 2026"
  },
  {
    id: 26,
    src: "/photos/5DB35B13-D27D-45FB-BDF5-678C5313110D_1_105_c.jpeg",
    alt: "Creative vision and artistic perspective",
    title: "Artistic Vision",
    category: "Artistic",
    date: "April 2026"
  },
  {
    id: 27,
    src: "/photos/9CB50DCA-32ED-47D0-8218-F63130444E20_1_105_c.jpeg",
    alt: "Modern life in the digital age",
    title: "Digital Age",
    category: "Contemporary",
    date: "April 2026"
  },
  {
    id: 28,
    src: "/photos/A236EBF7-771F-4055-BC4E-827B0FF9AA58_1_105_c.jpeg",
    alt: "Spontaneous laughter and joy",
    title: "Pure Joy",
    category: "Candid",
    date: "April 2026"
  },
  {
    id: 29,
    src: "/photos/A3679F2A-2419-44ED-8751-23100C4FB4A0_1_105_c.jpeg",
    alt: "Urban landscapes and cityscapes",
    title: "Cityscapes",
    category: "Urban",
    date: "April 2026"
  },
  {
    id: 30,
    src: "/photos/B6B410C9-9D0A-4717-AEDE-37DE304C65E6_1_105_c.jpeg",
    alt: "Action and movement captured",
    title: "Motion Capture",
    category: "Action",
    date: "April 2026"
  },
  {
    id: 31,
    src: "/photos/D4816D37-9859-4E6F-AE30-32CE88089DEB_1_105_c.jpeg",
    alt: "Peaceful moments in nature",
    title: "Nature's Calm",
    category: "Nature",
    date: "April 2026"
  },
  {
    id: 32,
    src: "/photos/D73EC716-7DC2-4673-9DE3-98448FF9D758_1_105_c.jpeg",
    alt: "Self-discovery through photography",
    title: "Self Discovery",
    category: "Self-Portrait",
    date: "April 2026"
  },
  {
    id: 33,
    src: "/photos/D96CBD30-A9FC-48D3-BE72-CF2E0CCA7721_1_105_c.jpeg",
    alt: "Documenting life's journey",
    title: "Life Journey",
    category: "Documentary",
    date: "April 2026"
  },
  {
    id: 34,
    src: "/photos/DD421A99-0FB1-412B-8C04-C46AD424EBDC_1_105_c.jpeg",
    alt: "Seasonal changes and transitions",
    title: "Seasonal Shift",
    category: "Seasonal",
    date: "April 2026"
  },
  {
    id: 35,
    src: "/photos/ED6207BA-3F75-4028-AC86-EBF74A0CEF59_1_105_c.jpeg",
    alt: "Urban nightlife and evening scenes",
    title: "Night Lights",
    category: "Urban",
    date: "April 2026"
  },
  {
    id: 36,
    src: "/photos/F89C2DC3-0CDF-43F4-A752-03F537105812_1_105_c.jpeg",
    alt: "Creative expressions and artistic freedom",
    title: "Creative Freedom",
    category: "Artistic",
    date: "April 2026"
  },
  {
    id: 37,
    src: "/photos/FC708B11-F05C-412F-BDDA-FA43C97EBD5F_1_105_c.jpeg",
    alt: "Modern lifestyle snapshots",
    title: "Modern Moments",
    category: "Lifestyle",
    date: "April 2026"
  }
];

const PhotoCard = ({ photo, index, onClick }: { photo: Photo; index: number; onClick?: () => void }) => {
  const cardRef = useRef<HTMLDivElement>(null);
  const [rotationDeg, setRotationDeg] = useState<number>(0);
  const [naturalSize, setNaturalSize] = useState<{ w: number; h: number } | null>(null);
  // Temporarily comment out useScroll
  // const { scrollYProgress } = useScroll({
  //   target: cardRef,
  //   offset: ["start end", "end start"]
  // });

  const springY = useSpring(0, { stiffness: 300, damping: 30 });
  const springOpacity = useSpring(1, { stiffness: 300, damping: 30 });
  const springScale = useSpring(1, { stiffness: 300, damping: 30 });

  const rotated90 = Math.abs(rotationDeg % 180) === 90;
  const isLandscape = naturalSize ? naturalSize.w > naturalSize.h : false;
  // Card is portrait-shaped; when the (possibly rotated) image is landscape, use `contain`
  // so it stays fully visible instead of being cropped by `cover`.
  const shouldContain = naturalSize ? (rotated90 ? !isLandscape : isLandscape) : rotated90;

  return (
    <div
      ref={cardRef}
      className="relative group mb-20 cursor-pointer"
      onClick={onClick}
    >
      <div className="relative overflow-hidden rounded-3xl bg-card border border-border card-light shadow-2xl hover:shadow-3xl transition-all duration-500">
        {/* Image */}
          <div className="relative aspect-square overflow-hidden">
          <div className="w-full h-full transition-transform duration-700 group-hover:scale-110">
            <img
              key={photo.src}
              src={photo.src}
              alt={photo.alt}
              className="w-full h-full"
              style={{
                transform: `rotate(${rotationDeg}deg)`,
                transformOrigin: "center",
                objectFit: shouldContain ? "contain" : "cover"
              }}
              loading="lazy"
              onError={(e) => {
                console.error('Failed to load image:', photo.src);
                e.currentTarget.style.display = 'none';
              }}
              onLoad={(e) => {
                const img = e.currentTarget;
                // Some browsers can have 0 natural size during failed loads; guard it.
                if (img.naturalWidth > 0 && img.naturalHeight > 0) {
                  setNaturalSize({ w: img.naturalWidth, h: img.naturalHeight });
                }
              }}
            />
          </div>

          {/* Enhanced Overlay */}
          <div className="absolute inset-0 bg-gradient-to-t from-black/90 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-all duration-500">
            <div className="absolute bottom-0 left-0 right-0 p-4 sm:p-6">
              <motion.div
                initial={{ y: 20, opacity: 0 }}
                whileInView={{ y: 0, opacity: 1 }}
                transition={{ delay: 0.2 }}
              >
                <div className="flex items-center gap-2 mb-3">
                  <Sparkles className="w-4 h-4 text-yellow-400" />
                  <span className="text-yellow-400 text-sm font-medium uppercase tracking-wider">
                    {photo.category}
                  </span>
                </div>
                <h3 className="text-white text-lg sm:text-xl font-bold mb-2 leading-tight">{photo.title}</h3>
                <p className="text-white/80 text-sm sm:text-base mb-4 leading-relaxed">{photo.alt}</p>
                {photo.date && (
                  <p className="text-white/60 text-xs sm:text-sm font-medium">{photo.date}</p>
                )}
              </motion.div>

              {/* Enhanced Action buttons */}
              <motion.div
                initial={{ y: 20, opacity: 0 }}
                whileInView={{ y: 0, opacity: 1 }}
                transition={{ delay: 0.4 }}
                className="flex flex-wrap gap-2 mt-4"
              >
                <Button size="sm" variant="secondary" className="bg-white/10 hover:bg-white/20 text-white border-white/20 backdrop-blur-sm flex-1 sm:flex-none transition-all duration-300 hover:scale-105">
                  <Heart className="w-4 h-4 mr-2" />
                  <span className="hidden sm:inline">Like</span>
                </Button>
                <Button
                  size="sm"
                  variant="secondary"
                  className="bg-white/10 hover:bg-white/20 text-white border-white/20 backdrop-blur-sm flex-1 sm:flex-none transition-all duration-300 hover:scale-105"
                  onClick={(e) => {
                    e.stopPropagation();
                    setRotationDeg((prev) => (prev + 90) % 360);
                  }}
                >
                  <RotateCw className="w-4 h-4 mr-2" />
                  <span className="hidden sm:inline">Rotate</span>
                </Button>
                <Button
                  size="sm"
                  variant="secondary"
                  className="bg-white/10 hover:bg-white/20 text-white border-white/20 backdrop-blur-sm flex-1 sm:flex-none transition-all duration-300 hover:scale-105"
                  onClick={(e) => {
                    e.stopPropagation();
                    try {
                      const link = document.createElement('a');
                      link.href = photo.src;
                      link.download = `${photo.title.replace(/\s+/g, '_').toLowerCase()}.jpg`;
                      link.target = '_blank';
                      document.body.appendChild(link);
                      link.click();
                      document.body.removeChild(link);
                    } catch (error) {
                      console.error('Download failed:', error);
                      window.open(photo.src, '_blank');
                    }
                  }}
                >
                  <Download className="w-4 h-4 mr-2" />
                  <span className="hidden sm:inline">Download</span>
                </Button>
                <Button size="sm" variant="secondary" className="bg-white/10 hover:bg-white/20 text-white border-white/20 backdrop-blur-sm flex-1 sm:flex-none transition-all duration-300 hover:scale-105">
                  <Share2 className="w-4 h-4 mr-2" />
                  <span className="hidden sm:inline">Share</span>
                </Button>
              </motion.div>
            </div>
          </div>
        </div>

        {/* Enhanced Floating elements */}
        <motion.div
          animate={{
            y: [0, -10, 0],
            rotate: [0, 5, 0],
            scale: [1, 1.1, 1],
          }}
          transition={{
            duration: 6,
            repeat: Infinity,
            delay: index * 0.5,
          }}
          className="absolute -top-3 -right-3 w-6 h-6 bg-gradient-to-br from-primary to-accent rounded-full opacity-20 blur-sm"
        />
        <motion.div
          animate={{
            y: [0, 10, 0],
            rotate: [0, -5, 0],
            scale: [1, 1.2, 1],
          }}
          transition={{
            duration: 8,
            repeat: Infinity,
            delay: index * 0.3,
          }}
          className="absolute -bottom-3 -left-3 w-4 h-4 bg-gradient-to-br from-accent to-secondary rounded-full opacity-30 blur-sm"
        />
        <motion.div
          animate={{
            rotate: 360,
            scale: [0.8, 1.2, 0.8],
          }}
          transition={{
            rotate: { duration: 20, repeat: Infinity, ease: "linear" },
            scale: { duration: 4, repeat: Infinity },
          }}
          className="absolute top-4 right-4 w-3 h-3 bg-yellow-400 rounded-full opacity-60"
        />
      </div>
    </div>
  );
};

const Photos = () => {
  const navigate = useNavigate();
  const containerRef = useRef<HTMLDivElement>(null);
  // Temporarily comment out useScroll
  // const { scrollYProgress } = useScroll({
  //   target: containerRef,
  //   offset: ["start start", "end end"]
  // });

  const [categoryFilter, setCategoryFilter] = useState<string>("All");
  const [searchQuery, setSearchQuery] = useState<string>("");
  const [activePhotoId, setActivePhotoId] = useState<number | null>(null);

  // Temporarily comment out scroll transforms
  // const backgroundY = useTransform(scrollYProgress, [0, 1], ["0%", "50%"]);
  // const headerOpacity = useTransform(scrollYProgress, [0, 0.3], [1, 0]);
  // const headerY = useTransform(scrollYProgress, [0, 0.3], [0, -50]);

  // Filter photos
  const allPhotos = photos;
  const categories = ["All", ...Array.from(new Set(photos.map((p) => p.category)))];

  const normalizedQuery = searchQuery.trim().toLowerCase();
  const filteredPhotos = photos.filter((p) => {
    const matchesCategory = categoryFilter === "All" ? true : p.category === categoryFilter;
    const matchesQuery =
      normalizedQuery.length === 0
        ? true
        : p.title.toLowerCase().includes(normalizedQuery) ||
          p.alt.toLowerCase().includes(normalizedQuery);
    return matchesCategory && matchesQuery;
  });

  const activePhoto = activePhotoId === null ? null : photos.find((p) => p.id === activePhotoId) ?? null;
  const activeIndex = filteredPhotos.findIndex((p) => p.id === activePhotoId);

  const openPhoto = (photo: Photo) => setActivePhotoId(photo.id);
  const closePhoto = () => setActivePhotoId(null);

  const goPrev = useCallback(() => {
    if (filteredPhotos.length === 0) return;
    const idx = activeIndex === -1 ? 0 : activeIndex;
    const prev = filteredPhotos[(idx - 1 + filteredPhotos.length) % filteredPhotos.length];
    setActivePhotoId(prev.id);
  }, [filteredPhotos, activeIndex]);

  const goNext = useCallback(() => {
    if (filteredPhotos.length === 0) return;
    const idx = activeIndex === -1 ? 0 : activeIndex;
    const next = filteredPhotos[(idx + 1) % filteredPhotos.length];
    setActivePhotoId(next.id);
  }, [filteredPhotos, activeIndex]);

  useEffect(() => {
    if (activePhotoId === null) return;
    // If the active photo disappears due to upload/remove/filter, close the modal.
    if (activeIndex === -1) setActivePhotoId(null);
  }, [activeIndex, activePhotoId]);

  useEffect(() => {
    if (activePhotoId === null) return;

    const prevOverflow = document.body.style.overflow;
    document.body.style.overflow = "hidden";

    return () => {
      document.body.style.overflow = prevOverflow;
    };
  }, [activePhotoId]);

  useEffect(() => {
    if (activePhotoId === null) return;

    const onKeyDown = (e: KeyboardEvent) => {
      if (e.key === "Escape") closePhoto();
      if (e.key === "ArrowLeft") goPrev();
      if (e.key === "ArrowRight") goNext();
    };

    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, [activePhotoId, goNext, goPrev]);

  return (
    <div ref={containerRef} className="min-h-screen bg-background">
      <h1 style={{color: 'red', fontSize: '24px'}}>Gallery Page Loaded</h1>
      {/* Animated Background */}
      {/* Temporarily comment out animated background
      <motion.div
        style={{ y: backgroundY }}
        className="fixed inset-0 bg-gradient-to-br from-primary/5 via-accent/5 to-secondary/5 pointer-events-none"
      />
      */}

      {/* Header */}
      {/* Temporarily comment out header animation
      <motion.header
        style={{ opacity: headerOpacity, y: headerY }}
        className="sticky top-0 z-40 glass border-b border-border/50 backdrop-blur-xl"
      >
      */}
      <header className="sticky top-0 z-40 glass border-b border-border/50 backdrop-blur-xl">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 py-4 sm:py-6">
          <div className="flex items-center justify-between">
            <Button
              variant="ghost"
              onClick={() => navigate("/")}
              className="flex items-center gap-2 hover:bg-accent/20"
            >
              <ArrowLeft className="w-4 h-4" />
              <span className="hidden sm:inline">Back to Portfolio</span>
            </Button>

            <div className="text-center">
              <h1 className="text-xl sm:text-2xl font-bold text-gradient">My Personal Gallery</h1>
              <p className="text-xs sm:text-sm text-muted-foreground mt-1">
                {allPhotos.length} memories • Scroll to explore
              </p>
            </div>

            <div className="flex gap-2 sm:gap-3">
              <Button
                onClick={() => navigate("/")}
                variant="outline"
                size="sm"
                className="hidden sm:flex"
              >
                <ArrowLeft className="w-4 h-4 mr-2" />
                Back
              </Button>
            </div>
          </div>
        </div>
      </header>

      {/* Hero Section */}
      <section className="relative py-32 px-6">
        <div className="max-w-4xl mx-auto text-center">
          <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 text-primary text-sm font-medium mb-8">
            <Heart className="w-4 h-4" />
            Personal Collection
          </div>

          <h2 className="text-5xl sm:text-6xl lg:text-7xl font-bold mb-6">
            My <span className="text-gradient">Personal</span> Journey
          </h2>

          <p className="text-xl text-muted-foreground max-w-2xl mx-auto mb-12">
            A visual diary of moments, memories, and experiences that define my story.
            Each photograph captures a piece of my journey through time.
          </p>

          <div className="flex justify-center">
            <div className="text-sm text-muted-foreground bg-card/50 backdrop-blur-sm rounded-full px-6 py-3 border border-border/50">
              <span className="text-primary font-medium">{allPhotos.length}</span> cherished memories • Scroll down to explore
            </div>
          </div>
        </div>

        {/* Floating elements */}
        <motion.div
          animate={{
            rotate: 360,
            scale: [1, 1.1, 1],
          }}
          transition={{
            rotate: { duration: 20, repeat: Infinity, ease: "linear" },
            scale: { duration: 4, repeat: Infinity },
          }}
          className="absolute top-20 left-10 w-16 h-16 bg-primary/10 rounded-full blur-xl"
        />
        <motion.div
          animate={{
            rotate: -360,
            scale: [1, 1.2, 1],
          }}
          transition={{
            rotate: { duration: 25, repeat: Infinity, ease: "linear" },
            scale: { duration: 5, repeat: Infinity },
          }}
          className="absolute bottom-20 right-10 w-20 h-20 bg-accent/10 rounded-full blur-xl"
        />
      </section>

      {/* Photo Gallery */}
      <section className="relative py-12 sm:py-20 px-4 sm:px-6">
        <div className="max-w-7xl mx-auto">
          {/* Category Filter */}
          <div className="flex flex-wrap justify-center gap-3 mb-12">
            {categories.map((category) => (
              <Button
                key={category}
                variant={categoryFilter === category ? "default" : "outline"}
                size="sm"
                onClick={() => setCategoryFilter(category)}
                className={`rounded-full transition-all duration-300 ${
                  categoryFilter === category
                    ? "bg-gradient-to-r from-primary to-accent text-white shadow-lg scale-105"
                    : "hover:bg-primary/10 hover:border-primary/50"
                }`}
              >
                {category}
              </Button>
            ))}
          </div>

          {/* Photo Count */}
          <div className="text-center mb-12">
            <div className="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-gradient-to-r from-primary/10 to-accent/10 backdrop-blur-sm border border-primary/20">
              <Camera className="w-5 h-5 text-primary" />
              <span className="text-primary font-semibold">
                {filteredPhotos.length} {filteredPhotos.length === 1 ? 'memory' : 'memories'}
                {categoryFilter !== "All" && ` in ${categoryFilter}`}
              </span>
            </div>
          </div>

          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8 md:gap-12">
            {filteredPhotos.map((photo, index) => (
              <PhotoCard 
                key={photo.id} 
                photo={photo} 
                index={index} 
                onClick={() => openPhoto(photo)}
              />
            ))}
          </div>
        </div>
      </section>

      {/* Footer */}
      <motion.footer
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.8 }}
        className="py-20 px-6 text-center"
      >
        <div className="max-w-2xl mx-auto">
          <h3 className="text-2xl font-bold mb-4">Thank You for Exploring</h3>
          <p className="text-muted-foreground mb-8">
            These photographs represent moments that matter to me. Each one tells a story,
            captures an emotion, or preserves a memory worth keeping.
          </p>

          <div className="flex justify-center gap-4">
            <Button onClick={() => navigate("/")} variant="outline">
              <ArrowLeft className="w-4 h-4 mr-2" />
              Back to Portfolio
            </Button>
            <Button>
              <Heart className="w-4 h-4 mr-2" />
              View More
            </Button>
          </div>
        </div>
      </motion.footer>

      {/* Enhanced Photo Modal */}
      <Dialog open={!!activePhoto} onOpenChange={closePhoto}>
        <DialogContent className="max-w-6xl w-full h-full max-h-[95vh] p-0 bg-black/95 border-0 backdrop-blur-xl">
          {activePhoto && (
            <div className="relative w-full h-full flex items-center justify-center">
              {/* Enhanced Close Button */}
              <Button
                variant="ghost"
                size="icon"
                className="absolute top-6 right-6 z-20 text-white hover:bg-white/20 backdrop-blur-sm rounded-full w-12 h-12 transition-all duration-300 hover:scale-110"
                onClick={closePhoto}
              >
                <X className="w-6 h-6" />
              </Button>

              {/* Enhanced Navigation Buttons */}
              <Button
                variant="ghost"
                size="icon"
                className="absolute left-6 top-1/2 -translate-y-1/2 z-20 text-white hover:bg-white/20 backdrop-blur-sm rounded-full w-12 h-12 transition-all duration-300 hover:scale-110"
                onClick={goPrev}
                disabled={filteredPhotos.length <= 1}
              >
                <ChevronLeft className="w-6 h-6" />
              </Button>

              <Button
                variant="ghost"
                size="icon"
                className="absolute right-6 top-1/2 -translate-y-1/2 z-20 text-white hover:bg-white/20 backdrop-blur-sm rounded-full w-12 h-12 transition-all duration-300 hover:scale-110"
                onClick={goNext}
                disabled={filteredPhotos.length <= 1}
              >
                <ChevronRight className="w-6 h-6" />
              </Button>

              {/* Enhanced Photo */}
              <motion.div
                className="relative max-w-full max-h-full"
                initial={{ opacity: 0, scale: 0.8 }}
                animate={{ opacity: 1, scale: 1 }}
                exit={{ opacity: 0, scale: 0.8 }}
                transition={{ duration: 0.4, ease: "easeOut" }}
              >
                <img
                  src={activePhoto.src}
                  alt={activePhoto.alt}
                  className="max-w-full max-h-full object-contain rounded-lg shadow-2xl"
                />
                {/* Subtle glow effect */}
                <div className="absolute inset-0 rounded-lg shadow-[0_0_60px_rgba(255,255,255,0.1)] pointer-events-none" />
              </motion.div>

              {/* Enhanced Photo Info */}
              <motion.div
                className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent p-8"
                initial={{ y: 50, opacity: 0 }}
                animate={{ y: 0, opacity: 1 }}
                transition={{ delay: 0.2, duration: 0.4 }}
              >
                <div className="max-w-2xl mx-auto text-center">
                  <div className="flex items-center justify-center gap-2 mb-4">
                    <Sparkles className="w-5 h-5 text-yellow-400" />
                    <span className="text-yellow-400 text-sm font-medium uppercase tracking-wider">
                      {activePhoto.category}
                    </span>
                  </div>
                  <h3 className="text-white text-2xl sm:text-3xl font-bold mb-3 leading-tight">{activePhoto.title}</h3>
                  <p className="text-white/90 text-base sm:text-lg mb-4 leading-relaxed">{activePhoto.alt}</p>
                  {activePhoto.date && (
                    <p className="text-white/70 text-sm sm:text-base font-medium mb-4">{activePhoto.date}</p>
                  )}
                  <div className="flex items-center justify-center gap-6 text-white/60 text-sm">
                    <span className="bg-white/10 backdrop-blur-sm px-4 py-2 rounded-full">
                      {activeIndex + 1} of {filteredPhotos.length}
                    </span>
                  </div>
                </div>
              </motion.div>
            </div>
          )}
        </DialogContent>
      </Dialog>
    </div>
  );
};

export default Photos;